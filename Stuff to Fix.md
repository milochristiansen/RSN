Stuff to Fix

* Look for a way to move the add feed form to the unread page. Currently it is in the footer for layout reasons.
* Add a mark all read button to the feed details.
* Add a way to delete read articles that are no longer in the feed.
* If an API call fails with a 403 recheck auth state immediately.
* Convert to a proper PWA
* Some database stuff should probably be in transactions. Namely cases where we check to see if something exists and create it if not.
